import { Gate } from "@laratype/auth";
import User  from "../models/User";

export default class {{GateName}} extends Gate {
  handle(actor: User, user: User): boolean {
    // Implement your authorization logic here
    // For example, allow if the user is updating their own profile
    return actor.id === user.id;
  }
}
